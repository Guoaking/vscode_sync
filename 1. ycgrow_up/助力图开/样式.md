## 尝试控件以及页面样式总结
### m css 单选 横向滚动 以及隐藏 滚动条
```css
//radio-div 的父级
.radio_gxsqfmnnw73y17k00 {
    display: flex;
    width: max-content;
}
////radio-div 的父级 的父级
#Cgxsqfmnnw73y17k00::-webkit-scrollbar {
    display: none;
    /* Chrome Safari */
}

#Cgxsqfmnnw73y17k00 {
    scrollbar-width: none;
    /* firefox */
    -ms-overflow-style: none;
    /* IE 10+ */
    overflow-x: auto;
    overflow-y: hidden;
}
```

### m css自动下拉
```
<Icon type="android-list"></Icon> 
```

### 图开控件 ivew菜单的数据结构
```json 
//  全局背景色冲突
[ {
      name: `首页`,
      icon: 'home',
      iconPosition: 'left',
      value: ``
    },
{
      name: `在线沟通`,
      icon: 'chatbubble-working',
      iconPosition: 'left',
      value: `/5ef090933bc25612397d1af0`
    },
{
      name: `我的工作组`,
      icon: 'navicon-round',
      iconPosition: 'left',
      value: `/467b7c601a9b3fba7bd765c84d553c97`
    },
{
      name: `我的BLOG`,
      icon: 'android-person',
      iconPosition: 'left',
      value: `/5ef07457c35e0256370a4cdc`
}]
```
### 图开控件 表格按钮 赋值
```json
//btns 
[{
        status: 'chat', 
        name: '聊天',
        show: true,
        disabled: false
      },{
        status: 'on', 
        name: '关注',
        show: true,
        disabled: false
}]

// 判断status
```

### 图开控件 ztree  赋值
```javascript
function(menulist,userlist){
  menulist = JSON.parse(menulist);
  userlist = JSON.parse(userlist);
  var lsit = [];
   for(var user of userlist){
   lsit.push(user.menu_id)
   }
var newlist = [];
 for(var menu of menulist){
 var map = {};
   map.id = menu._id;
   map.pid = menu.father_id;
   map.name = menu.name;
   if(lsit.indexOf(menu._id) > -1){
   map.checked = true;
   }
   newlist.push(map);
 }
  console.log(newlist)
return newlist;
}(#menulist#,#userlist#)
```
//调用方法  生成树
参数描述|参数类型|参数值 
-|-|-
对象数组|逻辑内部数据|  ?newlist
父id|自定义|？0
单个对象索引|自定义|?id
单个对象元素所属父id|自定义|?pid

```javascript
//把ztree 选中的数据 处理成[“id”,"id"]的形式存储
function(datas){
  var expandedKeys = [];
  
  function setName(datas){ //遍历树  获取id数组
  for(var i in datas){
    expandedKeys.push(datas[i].id)
    if(datas[i].children){
      setName(datas[i].children);
    }
  }
}
   setName(datas);
  
  return expandedKeys;
}(#datas#)

---

